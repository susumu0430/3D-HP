<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NEO KINETICS | Playful Personal Gym</title>
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Kanit:ital,wght@0,300;0,600;0,900;1,900&family=Noto+Sans+JP:wght@400;700;900&display=swap" rel="stylesheet">
    
    <!-- Three.js -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
    <!-- GSAP for Smooth Animations -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/ScrollTrigger.min.js"></script>

    <style>
        :root {
            --neon-green: #ccff00;
            --dark-bg: #050505;
        }

        body {
            background-color: var(--dark-bg);
            color: white;
            font-family: 'Noto Sans JP', sans-serif;
            overflow-x: hidden;
            cursor: none; /* Custom cursor */
        }

        /* Typography Override */
        .font-display {
            font-family: 'Kanit', sans-serif;
        }

        /* Custom Cursor */
        #cursor {
            position: fixed;
            top: 0;
            left: 0;
            width: 20px;
            height: 20px;
            background-color: var(--neon-green);
            border-radius: 50%;
            pointer-events: none;
            z-index: 9999;
            transform: translate(-50%, -50%);
            mix-blend-mode: difference;
            transition: width 0.3s, height 0.3s;
        }
        #cursor.hovered {
            width: 80px;
            height: 80px;
            background-color: transparent;
            border: 2px solid var(--neon-green);
        }

        /* 3D Canvas Container */
        #canvas-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100vh;
            z-index: -1;
            opacity: 0.6;
        }

        /* Utility Classes */
        .text-neon {
            color: var(--neon-green);
        }
        .bg-neon {
            background-color: var(--neon-green);
        }
        .text-outline {
            -webkit-text-stroke: 2px rgba(255,255,255,0.2);
            color: transparent;
        }
        .clip-text-image {
            background-clip: text;
            -webkit-background-clip: text;
            color: transparent;
            background-image: url('https://images.unsplash.com/photo-1534438327276-14e5300c3a48?q=80&w=2070&auto=format&fit=crop');
            background-size: cover;
            background-position: center;
        }

        /* Marquee Animation */
        .marquee-container {
            overflow: hidden;
            white-space: nowrap;
        }
        .marquee-content {
            display: inline-block;
            animation: marquee 20s linear infinite;
        }
        @keyframes marquee {
            0% { transform: translateX(0); }
            100% { transform: translateX(-50%); }
        }

        /* Loader */
        #loader {
            position: fixed;
            inset: 0;
            background: #000;
            z-index: 10000;
            display: flex;
            justify-content: center;
            align-items: center;
            transition: opacity 0.5s ease;
        }
    </style>
</head>
<body class="antialiased selection:bg-[#ccff00] selection:text-black">

    <!-- Custom Cursor -->
    <div id="cursor"></div>

    <!-- Loader -->
    <div id="loader">
        <div class="text-neon font-display text-4xl font-black italic tracking-tighter animate-pulse">LOADING...</div>
    </div>

    <!-- 3D Background -->
    <div id="canvas-container"></div>

    <!-- Navigation -->
    <nav class="fixed top-0 w-full z-50 px-6 py-6 flex justify-between items-center mix-blend-exclusion">
        <a href="#" class="text-2xl font-display font-black tracking-tighter hoverable">NEO KINETICS</a>
        <div class="hidden md:flex gap-8 text-sm font-bold tracking-widest">
            <a href="#concept" class="hover:text-[#ccff00] transition-colors hoverable">CONCEPT</a>
            <a href="#program" class="hover:text-[#ccff00] transition-colors hoverable">PROGRAM</a>
            <a href="#trainers" class="hover:text-[#ccff00] transition-colors hoverable">CREW</a>
        </div>
        <button class="border border-white px-6 py-2 rounded-full text-xs font-bold hover:bg-[#ccff00] hover:text-black hover:border-[#ccff00] transition-all duration-300 hoverable">
            BOOK TRIAL
        </button>
    </nav>

    <main>
        <!-- Hero Section -->
        <section class="relative h-screen flex flex-col justify-center items-center text-center px-4 overflow-hidden">
            <h1 class="font-display text-[12vw] leading-[0.8] font-black italic tracking-tighter relative z-10 parallax-bg" data-speed="0.2">
                REDEFINE<br>
                <span class="text-outline">YOURSELF</span>
            </h1>
            <p class="mt-8 text-lg md:text-xl font-bold tracking-widest text-gray-400 max-w-lg parallax-bg" data-speed="0.1">
                常識を捨てろ。身体を遊べ。<br>
                次世代の3Dボディメイクジム。
            </p>
            
            <div class="absolute bottom-10 left-0 w-full flex justify-center parallax-bg" data-speed="-0.1">
                <div class="w-[1px] h-20 bg-gradient-to-b from-[#ccff00] to-transparent animate-bounce"></div>
            </div>
        </section>

        <!-- Marquee Strip -->
        <div class="w-full bg-[#ccff00] text-black py-4 transform -skew-y-2 border-y-4 border-black z-20 relative">
            <div class="marquee-container">
                <div class="marquee-content font-display text-4xl font-black italic">
                    NO PAIN NO GAME — UNLOCK YOUR POTENTIAL — SWEAT IS DATA — PLAY HARD TRAIN HARD — NEO KINETICS — 
                    NO PAIN NO GAME — UNLOCK YOUR POTENTIAL — SWEAT IS DATA — PLAY HARD TRAIN HARD — NEO KINETICS —
                </div>
            </div>
        </div>

        <!-- Concept Section with Parallax Images -->
        <section id="concept" class="relative py-32 px-6 md:px-20 z-10">
            <div class="max-w-7xl mx-auto grid grid-cols-1 md:grid-cols-2 gap-20 items-center">
                <div class="relative group">
                    <!-- Image with parallax and hover distortion effect logic placeholder -->
                    <div class="overflow-hidden rounded-3xl transform transition-transform duration-700 hover:scale-95 border border-gray-800">
                        <img src="https://images.unsplash.com/photo-1534438327276-14e5300c3a48?q=80&w=2070&auto=format&fit=crop" 
                             alt="Gym Atmosphere" 
                             class="w-full h-[600px] object-cover filter grayscale contrast-125 group-hover:grayscale-0 transition-all duration-700 parallax-img" data-speed="0.1">
                    </div>
                    <!-- Floating Badge -->
                    <div class="absolute -bottom-10 -right-10 bg-black border border-[#ccff00] text-[#ccff00] w-32 h-32 rounded-full flex items-center justify-center font-display font-black text-xl animate-spin-slow hoverable">
                        LEVEL UP
                    </div>
                </div>

                <div class="space-y-8">
                    <span class="text-[#ccff00] font-display font-bold text-xl tracking-widest block mb-4">/// CONCEPT</span>
                    <h2 class="text-5xl md:text-7xl font-black leading-tight">
                        ただのジムは、<br>
                        もう終わり。
                    </h2>
                    <p class="text-gray-400 text-lg leading-relaxed">
                        NEO KINETICSは、「鍛える」を「遊ぶ」に進化させる。<br>
                        最新のバイオメカニクスと、没入感あふれる空間設計。<br>
                        まるでゲームの主人公のように、自分のステータスを書き換えろ。<br>
                        退屈なルーティンワークはここにはない。あるのは、熱狂と変化だけだ。
                    </p>
                    <button class="group relative px-8 py-4 bg-transparent border border-white text-white font-bold overflow-hidden hoverable mt-4">
                        <span class="relative z-10 group-hover:text-black transition-colors duration-300">READ MORE</span>
                        <div class="absolute inset-0 bg-[#ccff00] transform scale-x-0 group-hover:scale-x-100 transition-transform origin-left duration-300 ease-out"></div>
                    </button>
                </div>
            </div>
        </section>

        <!-- Program Section (Horizontal Scroll Feel) -->
        <section id="program" class="py-32 bg-[#0a0a0a] relative z-10 border-t border-gray-900">
            <div class="container mx-auto px-6">
                <h2 class="text-[#ccff00] font-display text-[10vw] font-black leading-none opacity-20 absolute top-10 left-0 select-none">PROGRAMS</h2>
                
                <div class="relative z-10 mt-20 grid grid-cols-1 md:grid-cols-3 gap-8">
                    <!-- Card 1 -->
                    <div class="bg-[#111] p-8 border border-gray-800 rounded-2xl hover:border-[#ccff00] transition-colors duration-300 group hoverable transform hover:-translate-y-2">
                        <div class="text-4xl mb-4 group-hover:scale-110 transition-transform duration-300">⚡</div>
                        <h3 class="font-display text-3xl font-black italic mb-2">SPEED &<br>AGILITY</h3>
                        <p class="text-gray-500 text-sm mb-6">瞬発力を極限まで高める高強度インターバルトレーニング。</p>
                        <div class="w-full h-[200px] overflow-hidden rounded-lg">
                            <img src="https://images.unsplash.com/photo-1599058945522-28d584b6f0ff?q=80&w=2069&auto=format&fit=crop" class="w-full h-full object-cover grayscale group-hover:grayscale-0 transition-all duration-500">
                        </div>
                    </div>

                    <!-- Card 2 -->
                    <div class="bg-[#111] p-8 border border-gray-800 rounded-2xl hover:border-[#ccff00] transition-colors duration-300 group hoverable transform hover:-translate-y-2 md:mt-12">
                        <div class="text-4xl mb-4 group-hover:scale-110 transition-transform duration-300">💪</div>
                        <h3 class="font-display text-3xl font-black italic mb-2">HYPERTROPHY<br>HACK</h3>
                        <p class="text-gray-500 text-sm mb-6">科学的アプローチで筋肉を最短距離で破壊・再生する。</p>
                        <div class="w-full h-[200px] overflow-hidden rounded-lg">
                            <img src="https://images.unsplash.com/photo-1583454110551-21f2fa2afe61?q=80&w=2070&auto=format&fit=crop" class="w-full h-full object-cover grayscale group-hover:grayscale-0 transition-all duration-500">
                        </div>
                    </div>

                    <!-- Card 3 -->
                    <div class="bg-[#111] p-8 border border-gray-800 rounded-2xl hover:border-[#ccff00] transition-colors duration-300 group hoverable transform hover:-translate-y-2 md:mt-24">
                        <div class="text-4xl mb-4 group-hover:scale-110 transition-transform duration-300">🧘</div>
                        <h3 class="font-display text-3xl font-black italic mb-2">MIND &<br>FLOW</h3>
                        <p class="text-gray-500 text-sm mb-6">動的瞑想。身体操作を通じてメンタルをチューニングする。</p>
                        <div class="w-full h-[200px] overflow-hidden rounded-lg">
                            <img src="https://images.unsplash.com/photo-1518310383802-640c2de311b2?q=80&w=2070&auto=format&fit=crop" class="w-full h-full object-cover grayscale group-hover:grayscale-0 transition-all duration-500">
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- CTA Section -->
        <section class="relative h-[80vh] flex items-center justify-center overflow-hidden z-10">
            <div class="absolute inset-0 z-0">
                <img src="https://images.unsplash.com/photo-1571902943202-507ec2618e8f?q=80&w=1975&auto=format&fit=crop" class="w-full h-full object-cover opacity-30">
                <div class="absolute inset-0 bg-gradient-to-t from-black via-transparent to-black"></div>
            </div>
            
            <div class="relative z-10 text-center space-y-8">
                <h2 class="font-display text-6xl md:text-9xl font-black italic text-white mix-blend-overlay hoverable">
                    START THE<br>REVOLUTION
                </h2>
                <div class="flex justify-center">
                    <a href="#" class="group relative inline-flex items-center justify-center px-12 py-6 overflow-hidden font-bold text-black transition-all duration-300 bg-[#ccff00] rounded-full hover:bg-white hover:scale-105 hoverable">
                        <span class="absolute w-0 h-0 transition-all duration-500 ease-out bg-white rounded-full group-hover:w-80 group-hover:h-80 opacity-10"></span>
                        <span class="relative text-xl tracking-widest">無料体験を予約する</span>
                        <svg class="w-6 h-6 ml-2 relative" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path></svg>
                    </a>
                </div>
            </div>
        </section>

        <!-- Footer -->
        <footer class="bg-black py-12 border-t border-gray-900 relative z-20">
            <div class="container mx-auto px-6 flex flex-col md:flex-row justify-between items-end">
                <div>
                    <h3 class="font-display text-3xl font-black text-white mb-2">NEO KINETICS</h3>
                    <p class="text-gray-500 text-sm">Created for the Future of Fitness.</p>
                </div>
                <div class="mt-8 md:mt-0 flex gap-6 text-gray-400">
                    <a href="#" class="hover:text-[#ccff00] hoverable">INSTAGRAM</a>
                    <a href="#" class="hover:text-[#ccff00] hoverable">YOUTUBE</a>
                    <a href="#" class="hover:text-[#ccff00] hoverable">CONTACT</a>
                </div>
            </div>
        </footer>
    </main>

    <script>
        // --- 1. Custom Cursor Logic ---
        const cursor = document.getElementById('cursor');
        const hoverables = document.querySelectorAll('.hoverable');

        document.addEventListener('mousemove', (e) => {
            cursor.style.left = e.clientX + 'px';
            cursor.style.top = e.clientY + 'px';
        });

        hoverables.forEach(el => {
            el.addEventListener('mouseenter', () => cursor.classList.add('hovered'));
            el.addEventListener('mouseleave', () => cursor.classList.remove('hovered'));
        });

        // --- 2. Loader ---
        window.addEventListener('load', () => {
            const loader = document.getElementById('loader');
            setTimeout(() => {
                loader.style.opacity = '0';
                setTimeout(() => {
                    loader.style.display = 'none';
                    initThreeJS(); // Start 3D after load
                }, 500);
            }, 1000);
        });

        // --- 3. GSAP Parallax Logic ---
        gsap.registerPlugin(ScrollTrigger);

        // Text Parallax
        gsap.utils.toArray('.parallax-bg').forEach(layer => {
            const speed = layer.getAttribute('data-speed');
            gsap.to(layer, {
                y: (i, target) => ScrollTrigger.maxScroll(window) * speed,
                ease: "none",
                scrollTrigger: {
                    trigger: "body",
                    start: "top top",
                    end: "bottom bottom",
                    scrub: 0
                }
            });
        });

        // Image Parallax within Container
        gsap.utils.toArray('.parallax-img').forEach(img => {
            gsap.to(img, {
                y: -50,
                scale: 1.1,
                ease: "none",
                scrollTrigger: {
                    trigger: img.parentElement,
                    start: "top bottom",
                    end: "bottom top",
                    scrub: true
                }
            });
        });


        // --- 4. Three.js Background (The "Overwhelming Quality" Part) ---
        function initThreeJS() {
            const container = document.getElementById('canvas-container');
            
            // Scene Setup
            const scene = new THREE.Scene();
            // Fog for depth
            scene.fog = new THREE.FogExp2(0x050505, 0.002);

            const camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1000);
            camera.position.z = 30;

            const renderer = new THREE.WebGLRenderer({ alpha: true, antialias: true });
            renderer.setSize(window.innerWidth, window.innerHeight);
            renderer.setPixelRatio(window.devicePixelRatio);
            container.appendChild(renderer.domElement);

            // Objects: Geometric Particles Field
            const particlesGeometry = new THREE.BufferGeometry();
            const particlesCount = 2000;
            
            const posArray = new Float32Array(particlesCount * 3);
            const randomArray = new Float32Array(particlesCount); // For animation variation

            for(let i = 0; i < particlesCount * 3; i++) {
                posArray[i] = (Math.random() - 0.5) * 100; // Spread wide
            }
            for(let i = 0; i < particlesCount; i++) {
                randomArray[i] = Math.random();
            }

            particlesGeometry.setAttribute('position', new THREE.BufferAttribute(posArray, 3));
            particlesGeometry.setAttribute('aRandom', new THREE.BufferAttribute(randomArray, 1));

            // Custom Shader Material for Neon Pulse Effect
            const particlesMaterial = new THREE.ShaderMaterial({
                transparent: true,
                depthWrite: false,
                blending: THREE.AdditiveBlending,
                uniforms: {
                    uTime: { value: 0 },
                    uColor: { value: new THREE.Color('#ccff00') },
                    uMouse: { value: new THREE.Vector2(0, 0) }
                },
                vertexShader: `
                    uniform float uTime;
                    uniform vec2 uMouse;
                    attribute float aRandom;
                    varying float vAlpha;
                    
                    void main() {
                        vec3 pos = position;
                        
                        // Movement Logic: Wave + Mouse Repulsion
                        float time = uTime * 0.5;
                        
                        // Vertical wavy motion
                        pos.y += sin(time + pos.x * 0.1) * 2.0;
                        pos.x += cos(time + pos.y * 0.1) * 1.0;
                        
                        // Mouse interaction (simple distortion)
                        float dist = distance(pos.xy, uMouse * 50.0); // Adjust scale
                        float influence = smoothstep(20.0, 0.0, dist);
                        pos.z += influence * 15.0; // Push towards camera on mouse hover
                        
                        vec4 mvPosition = modelViewMatrix * vec4(pos, 1.0);
                        gl_Position = projectionMatrix * mvPosition;
                        
                        // Size variation
                        gl_PointSize = (4.0 * aRandom + 2.0) * (15.0 / -mvPosition.z);
                        
                        // Fade distant particles
                        vAlpha = smoothstep(50.0, 0.0, -mvPosition.z / 2.0);
                    }
                `,
                fragmentShader: `
                    uniform vec3 uColor;
                    varying float vAlpha;
                    
                    void main() {
                        // Circular particle
                        float r = distance(gl_PointCoord, vec2(0.5));
                        if (r > 0.5) discard;
                        
                        // Soft glow center
                        float glow = 1.0 - (r * 2.0);
                        glow = pow(glow, 2.0);
                        
                        gl_FragColor = vec4(uColor, vAlpha * glow);
                    }
                `
            });

            const particlesMesh = new THREE.Points(particlesGeometry, particlesMaterial);
            scene.add(particlesMesh);

            // Add floating geometric shapes (Icosahedrons) for "Physical" feel
            const shapesGroup = new THREE.Group();
            const geometry = new THREE.IcosahedronGeometry(1, 0);
            const material = new THREE.MeshBasicMaterial({ 
                color: 0xccff00, 
                wireframe: true,
                transparent: true,
                opacity: 0.1
            });

            for (let i = 0; i < 15; i++) {
                const mesh = new THREE.Mesh(geometry, material);
                mesh.position.set(
                    (Math.random() - 0.5) * 60,
                    (Math.random() - 0.5) * 40,
                    (Math.random() - 0.5) * 30
                );
                mesh.scale.setScalar(Math.random() * 3 + 1);
                mesh.userData = {
                    rotationSpeed: Math.random() * 0.02,
                    floatSpeed: Math.random() * 0.01 + 0.005,
                    yOffset: Math.random() * Math.PI * 2
                };
                shapesGroup.add(mesh);
            }
            scene.add(shapesGroup);


            // Event Listeners
            let mouseX = 0;
            let mouseY = 0;
            let targetX = 0;
            let targetY = 0;

            const windowHalfX = window.innerWidth / 2;
            const windowHalfY = window.innerHeight / 2;

            document.addEventListener('mousemove', (event) => {
                mouseX = (event.clientX - windowHalfX);
                mouseY = (event.clientY - windowHalfY);
                
                // For Shader
                particlesMaterial.uniforms.uMouse.value.x = (event.clientX / window.innerWidth) * 2 - 1;
                particlesMaterial.uniforms.uMouse.value.y = -(event.clientY / window.innerHeight) * 2 + 1;
            });

            window.addEventListener('resize', () => {
                camera.aspect = window.innerWidth / window.innerHeight;
                camera.updateProjectionMatrix();
                renderer.setSize(window.innerWidth, window.innerHeight);
            });

            // Animation Loop
            const clock = new THREE.Clock();

            function animate() {
                requestAnimationFrame(animate);

                const elapsedTime = clock.getElapsedTime();
                targetX = mouseX * 0.001;
                targetY = mouseY * 0.001;

                // Rotate Scene slightly based on mouse
                particlesMesh.rotation.y += 0.05 * (targetX - particlesMesh.rotation.y);
                particlesMesh.rotation.x += 0.05 * (targetY - particlesMesh.rotation.x);

                // Update Uniforms
                particlesMaterial.uniforms.uTime.value = elapsedTime;

                // Animate Floating Shapes
                shapesGroup.children.forEach(mesh => {
                    mesh.rotation.x += mesh.userData.rotationSpeed;
                    mesh.rotation.y += mesh.userData.rotationSpeed;
                    mesh.position.y += Math.sin(elapsedTime + mesh.userData.yOffset) * mesh.userData.floatSpeed;
                });
                
                // Parallax Camera movement based on Scroll
                const scrollY = window.scrollY;
                camera.position.y = -scrollY * 0.01; // Move camera down as we scroll
                camera.position.z = 30 + (scrollY * 0.005); // Subtle zoom out/in

                renderer.render(scene, camera);
            }

            animate();
        }
    </script>
</body>
</html>
